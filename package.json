{
  "name": "syncia",
  "scripts": {
    "sync:dev": "nodemon --exec 'rsync -avz $(pwd) service@srgl.cc:/var/code'",
    "node:dev": "cd node && yarn run ix:dev"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
