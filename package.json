{
  "name": "syncia",
  "scripts": {
    "sync:dev": "nodemon --exec 'rsync  -av $(pwd) zergal@srgl.cc:/var/code'",
    "node:dev": "cd node && npm run ix:dev",
    "web:dev": "npx sirv-cli web --dev"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
